package learningjava;

import javax.swing.BorderFactory;
import javax.swing.ImageIcon;
import javax.swing.JFrame;
import javax.swing.JLabel;
import javax.swing.JOptionPane;
import javax.swing.JPanel;
import javax.swing.border.Border;
import java.awt.Color;
import java.awt.Font;
import java.util.Scanner;

@SuppressWarnings("unused")

public class Main {
	public static void main(String[] args) {

		myframe frame = new myframe();

		// JFrame mainframe = new JFrame(); // creating a frame or screen of the app
		// mainframe.setTitle("TestFrame"); // title of the frane
		// mainframe.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); // if you do not
		// set this to exit on close it will
		// remain a background proccess
		// mainframe.setSize(1200, 800); // size(resolution) of the app/frame
		// mainframe.setVisible(true); // setting its visibility to true otherwise will
		// not show.
		// mainframe.getContentPane().setBackground(Color.DARK_GRAY); // setting
		// background colour of screen of app/frame
		// mainframe.add(label); // adding a label
		// mainframe.setLayout(null); // setting the bounds/layout

	}
}
